import 'package:flutter/material.dart';


class Contoh_Widgets extends StatelessWidget {
  const Contoh_Widgets({Key? key}) : super(key: key);

//! Widget Ini biasanya dipake buat tampilan card(contoh)
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
